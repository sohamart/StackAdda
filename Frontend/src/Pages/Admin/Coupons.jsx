import { useEffect, useState } from "react";
import { Loader2, Plus, Tag, Trash2 } from "lucide-react";
import { toast } from "react-toastify";
import API from "../../api/axios";

const initialForm = { code: "", discountType: "percentage", discountValue: "", minimumAmount: 0, maxDiscount: 0, usageLimit: 0, validTill: "" };
export default function Coupons() {
  const [coupons, setCoupons] = useState([]); const [form, setForm] = useState(initialForm); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false);
  const load = async () => { try { const { data } = await API.get("/coupon"); setCoupons(data.coupons || []); } catch (e) { toast.error(e.response?.data?.message || "Could not load coupons."); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  const create = async (event) => { event.preventDefault(); try { setSaving(true); const { data } = await API.post("/coupon", { ...form, discountValue: Number(form.discountValue), validTill: new Date(form.validTill).toISOString() }); toast.success(data.message); setForm(initialForm); load(); } catch (e) { toast.error(e.response?.data?.message || "Could not create coupon."); } finally { setSaving(false); } };
  const remove = async (id) => { if (!window.confirm("Delete this coupon?")) return; try { await API.delete(`/coupon/${id}`); toast.success("Coupon deleted."); load(); } catch (e) { toast.error(e.response?.data?.message || "Could not delete coupon."); } };
  if (loading) return <div className="flex h-[60vh] items-center justify-center"><Loader2 className="animate-spin text-orange-500" size={46}/></div>;
  return <section className="space-y-7"><header><h1 className="flex items-center gap-3 text-3xl font-bold text-white"><Tag className="text-orange-500"/> Coupons</h1><p className="mt-2 text-white/55">Create and manage course discount codes.</p></header><form onSubmit={create} className="grid gap-3 rounded-3xl border border-white/10 bg-white/[.045] p-5 backdrop-blur-2xl md:grid-cols-2 xl:grid-cols-4"><input required value={form.code} onChange={(e) => setForm({...form, code:e.target.value.toUpperCase()})} placeholder="Code e.g. STACK20" className="rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-white outline-none focus:border-orange-500"/><select value={form.discountType} onChange={(e) => setForm({...form, discountType:e.target.value})} className="rounded-xl border border-white/10 bg-zinc-900 px-4 py-3 text-white"><option value="percentage">Percentage</option><option value="fixed">Fixed amount</option></select><input required min="1" type="number" value={form.discountValue} onChange={(e) => setForm({...form, discountValue:e.target.value})} placeholder="Discount value" className="rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-white"/><input required type="datetime-local" value={form.validTill} onChange={(e) => setForm({...form, validTill:e.target.value})} className="rounded-xl border border-white/10 bg-zinc-900 px-4 py-3 text-white"/><input min="0" type="number" value={form.minimumAmount} onChange={(e) => setForm({...form, minimumAmount:e.target.value})} placeholder="Minimum amount" className="rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-white"/><input min="0" type="number" value={form.maxDiscount} onChange={(e) => setForm({...form, maxDiscount:e.target.value})} placeholder="Max discount (0 = none)" className="rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-white"/><input min="0" type="number" value={form.usageLimit} onChange={(e) => setForm({...form, usageLimit:e.target.value})} placeholder="Usage limit (0 = unlimited)" className="rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-white"/><button disabled={saving} className="flex items-center justify-center gap-2 rounded-xl bg-orange-500 px-4 py-3 font-medium text-white hover:bg-orange-600 disabled:opacity-60"><Plus size={18}/> Create coupon</button></form><div className="overflow-hidden rounded-3xl border border-white/10 bg-white/[.045] backdrop-blur-2xl"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead className="border-b border-white/10 text-xs uppercase tracking-wider text-white/40"><tr><th className="p-5">Code</th><th>Discount</th><th>Usage</th><th>Expiry</th><th>Status</th><th></th></tr></thead><tbody>{coupons.map((coupon) => <tr key={coupon._id} className="border-b border-white/5 text-sm"><td className="p-5 font-semibold text-orange-300">{coupon.code}</td><td className="text-white">{coupon.discountType === "percentage" ? `${coupon.discountValue}%` : `₹${coupon.discountValue}`}</td><td className="text-white/60">{coupon.usedCount} / {coupon.usageLimit || "∞"}</td><td className="text-white/60">{new Date(coupon.validTill).toLocaleDateString()}</td><td><span className={`rounded-full px-3 py-1 text-xs ${coupon.isActive && new Date(coupon.validTill) > new Date() ? "bg-green-500/15 text-green-300" : "bg-red-500/15 text-red-300"}`}>{coupon.isActive ? "Active" : "Inactive"}</span></td><td className="p-5"><button onClick={() => remove(coupon._id)} className="text-red-400 hover:text-red-300"><Trash2 size={18}/></button></td></tr>)}{!coupons.length && <tr><td colSpan="6" className="p-12 text-center text-white/45">No coupons created yet.</td></tr>}</tbody></table></div></div></section>;
}
