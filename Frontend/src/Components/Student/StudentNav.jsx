import { NavLink } from "react-router-dom";
import { BookOpen, LayoutDashboard, ReceiptText, User, WalletCards } from "lucide-react";
const links = [["/student", "Overview", LayoutDashboard, true], ["/student/courses", "My Courses", BookOpen], ["/student/orders", "Orders", ReceiptText], ["/student/payments", "Payments", WalletCards], ["/student/profile", "Profile", User]];
export default function StudentNav() { return <nav className="mb-8 flex gap-2 overflow-x-auto rounded-2xl border border-white/10 bg-white/[.035] p-2 backdrop-blur-2xl no-scrollbar">{links.map(([to, label, Icon, end]) => <NavLink key={to} to={to} end={end} className={({isActive}) => `flex shrink-0 items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-medium transition ${isActive ? "bg-orange-500 text-white" : "text-white/60 hover:bg-white/10 hover:text-white"}`}><Icon size={17}/>{label}</NavLink>)}</nav>; }
